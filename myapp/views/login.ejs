<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
	<%- include("./partials/head") %>
<body>
	<!-- Header -->
	<%- include("./partials/header_deslogueado") %>
	<!-- /Header -->

	<!-- Novedades -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Ingresar</h2>
			</div>
			<!-- Show the "ultimos" products -->
			<h2 class="col-12"> 
				<form class="form" action="/users/login" method="POST">
					<div>
						<input id="user" type="text" name="usuario" value="" placeholder="Usuario" required>
						<% if (locals.errors && errors.usuario) { %>
						 <p class="error"><%= errors.usuario.msg %></p>
						<% } %>
					</div>
				 
					<div>
						<input id="pass" type="password" name="pass" value="" placeholder="Ingrese su contraseña" required>
					</div>
				 
					<div>
						<input id="recordarme" type="checkbox" name="login" value=""><label>Recordarme</label>
					</div>
				 
					<button type="submit">Enviar</button>
				 </form>
			</h2>

	<!-- footer -->
	<%- include("./partials/footer") %>
	<!-- /footer -->
</body>

</html>